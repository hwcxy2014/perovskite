# this code calculates the expected number of times to pick the
# maximum value from a list of values without replacement

# length of the list
n = 72
expN = 0
for i in range(1,n+1):
	prob = float(i)/
	expN += i *
